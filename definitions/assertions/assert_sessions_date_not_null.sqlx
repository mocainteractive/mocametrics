config {
  type: "assertion",
  description: "Tutte le sessioni devono avere una data non nulla. NULL indica un problema nella conversione del timestamp in stg_events_sessionized.",
  tags: ["assertions", "sessions"]
}

SELECT *
FROM ${ref("mart_sessions")}
WHERE date IS NULL
